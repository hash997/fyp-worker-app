import React from "react";
import { View, Text } from "react-native";
import { AppStyles } from "../AppStyles";

const SendOfferScreen = () => {
  return (
    <View style={AppStyles.container}>
      <Text>send offer screen</Text>
    </View>
  );
};

export default SendOfferScreen;
